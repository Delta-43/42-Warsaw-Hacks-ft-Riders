[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/generate_config.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[[services]]
name = "backend"
rootDirectory = "backend"
